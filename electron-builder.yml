appId: com.patrykku.quviu
productName: Quviu
directories:
    buildResources: build
files:
    - '!**/.vscode/*'
    - '!src/*'
    - '!electron.vite.config.{js,ts,mjs,cjs}'
    - '!{.eslintignore,.eslintrc.cjs,.prettierignore,.prettierrc.yaml,dev-app-update.yml,CHANGELOG.md,README.md,biome.json,.gitignore,.editorconfig}'
    - '!{.env,.env.*,.npmrc,pnpm-lock.yaml,.npmrc}'
icon: 'build/icon.png'
fileAssociations:
    - ext: mp3, wav, flac, m4a, opus
      name: Audio Files
      role: Editor
    - ext: mp4, mkv, avi, mov, webm
      name: Video Files
      role: Editor
asarUnpack:
    - resources/**
    - node_modules/fluent-ffmpeg
    - out/main/chunks/*.node

win:
    executableName: quviu
    target:
        - nsis
        - portable
    artifactName: ${name}-${version}-win-${arch}.${ext}
nsis:
    artifactName: ${name}-${version}-win-${arch}-setup.${ext}
    shortcutName: ${productName}
    uninstallDisplayName: ${productName}
    createDesktopShortcut: always
portable:
    artifactName: "${name}-${version}-win-${arch}-portable.${ext}"

mac:
    entitlementsInherit: build/entitlements.mac.plist
    extendInfo:
        - NSDocumentsFolderUsageDescription: Application requests access to the user's Documents folder.
        - NSDownloadsFolderUsageDescription: Application requests access to the user's Downloads folder.
    notarize: false
    artifactName: ${name}-${version}-mac-${arch}.${ext}
dmg:
    artifactName: ${name}-${version}-mac-${arch}.${ext}

linux:
    target:
        - deb
        - rpm
        - pacman
        - AppImage
    category: Utility
    artifactName: ${name}-${version}-linux-${arch}.${ext}
deb:
    depends:
     - ffmpeg
    artifactName: ${name}-${version}-linux-${arch}.${ext}

rpm:
    depends:
     - ffmpeg
    artifactName: ${name}-${version}-linux-${arch}.${ext}

pacman:
    depends:
     - ffmpeg
    artifactName: ${name}-${version}-linux-${arch}.${ext}
appImage:
    artifactName: ${name}-${version}-linux-${arch}.${ext}
npmRebuild: false
